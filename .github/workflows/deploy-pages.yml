name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          # UPGRADED: 3.27.1 supports WidgetStateProperty used by Telegram SDK
          flutter-version: '3.27.1'
          channel: 'stable'

      - name: Install Dependencies
        run: |
          cd client
          flutter pub get

      - name: Build Web
        run: |
          cd client
          # Added NEXUS_DEV=true and ensured release mode
          flutter build web --release --base-href "/nexus-core/" --dart-define=NEXUS_DEV=true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: client/build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4     