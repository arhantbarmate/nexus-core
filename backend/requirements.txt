# ====================================================
# NEXUS PROTOCOL: BACKEND REQUIREMENTS (v1.4.0)
# ====================================================
# Phase 1.4.0 Scope:
# - Single Sovereign Node
# - Deterministic execution
# - Local-first storage
# - Secure ingress (Cloudflare)
# ====================================================

# --- Core Web Server & Gateway ---
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9
python-dotenv==1.0.0
pydantic==2.6.0

# --- Networking & Adapters ---
# httpx is preferred for async adapters (Phase 2.0 readiness)
httpx==0.26.0
# requests retained for simple sync adapters and tooling
requests==2.31.0

# --- Development Utilities ---
# Used only for local development / hot reload (NOT production)
watchfiles==0.21.0

# --- Performance & Concurrency ---
# STAGED: Not active in Phase 1.x
# Prepared for future async DB access in Phase 2.0
aiosqlite==0.19.0

# --- Testing & Security Auditing ---
pytest==8.0.0
pytest-asyncio==0.23.5

# NOTE: 'safety' removed due to Pydantic v2 conflict. 
# It should be run in a separate CI/CD environment, not in the production build.