# --- CORE ENGINE ---
fastapi==0.110.0
uvicorn[standard]==0.27.1

# --- CONFIGURATION & SECURITY ---
# Essential for loading .env variables without hardcoding secrets
python-dotenv==1.0.1
# Recommended for production-grade settings management
pydantic-settings==2.2.1

# --- DATABASE & UTILITIES ---
# Standard for professional SQL handling, even with SQLite
sqlalchemy==2.0.29
# Modern HTTP client for your TON Builders handshake
httpx==0.27.0

# --- TESTING (GRANT-READY) ---
pytest==8.1.1
# Required to test the 'async' endpoints in your Brain
pytest-asyncio==0.23.6