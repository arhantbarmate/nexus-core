name: nexus_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  # 💎 Extra explicit SDK range for maximum compatibility
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # ✅ Corrected versioning
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # 📋 Assets configuration
  # Ensure the logo is placed in client/assets/nexus-logo.png
  assets:
    - assets/nexus-logo.png
