name: nexus_app
description: "Nexus Protocol - Sovereign Edge Gateway Client (Phase 1.4.0)"
publish_to: 'none'

version: 1.4.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 1. NETWORK LAYER
  # Hardened for persistent connections
  http: ^1.2.0

  # 2. BRIDGE LAYER
  # Essential for Custom Triad Bridge (tg_bridge_web.dart)
  js: ^0.7.2

  # 3. UI AESTHETICS
  google_fonts: ^7.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/nexus-logo.png