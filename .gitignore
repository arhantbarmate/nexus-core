# ==============================================================================
# NEXUS PROTOCOL - MASTER GITIGNORE (PHASE 1.3.1 HARDENED)
# ==============================================================================

# --- 🔐 SENSITIVE DATA (CRITICAL) ---
.env
.env.*
*.properties
local.properties
*.jks
*.keystore
secrets.json

# --- 🛠️ TOOLS & BINARIES ---
ngrok.exe
ngrok
*.exe
*.dll
*.so
*.dylib

# --- 🛰️ NEXUS SOVEREIGN VAULT (HARDENED) ---
# Primary Logic: Catch the master ledger
**/nexus_vault.db*
**/*.db-journal
**/*.db-wal
**/*.db-shm

# --- 🔒 LOCAL RUNTIME STATE (OPTIONAL REFINEMENT) ---
# Secondary Logic: Catch accidental sqlite clones/temp files
*.sqlite
*.sqlite3

# --- 🧠 PYTHON / FASTAPI (BRAIN) ---
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.venv/
venv/
env/

# Scoped ignores (Does not touch lib/ source code)
backend/dist/
backend/build/
backend/*.egg-info/

# --- 📱 FLUTTER / DART (BODY) ---
# We keep client/lib/ tracked, but ignore the compiler's machinery
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/

# Recursive build outputs
**/build/

# Flutter Web & Chrome Artifacts (CI Stability)
**/.dart_tool/chrome-device/
**/web/index.html.tmp
**/web/flutter_bootstrap.js

# OS/Platform ephemeral artifacts
**/linux/flutter/ephemeral/
**/windows/flutter/ephemeral/
**/ios/Flutter/.last_build_id
**/ios/Flutter/App.framework

# --- 📊 LOGS & TELEMETRY ---
*.log
logs/
stress_results.json
nexus_sentry.log

# --- 💻 IDE / SYSTEM ---
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
desktop.ini
*.user
*.suo
*.sln

# --- 🗑️ JUNK & GHOST ARTIFACTS ---
mkdir
*.tmp
temp/